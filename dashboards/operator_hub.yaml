# Dashboard: Operator Hub
# Version: 0.1.0
# Notes:
# - MVP dashboard shell for the new custom operator app.
# - Fachmodule remain authoritative in existing YAML dashboards until migrated.
title: Operator Hub
views:
  - title: Hub
    path: home
    icon: mdi:view-dashboard-outline
    type: sections
    max_columns: 1
    sections:
      - type: grid
        cards:
          - type: custom:sieker-operator-hub
            title: Operator Hub
            default_page: home
